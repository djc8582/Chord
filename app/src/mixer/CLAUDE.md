# mixer

> **Tier 3** — Depends on `document-model`, `inspector`.

## What This Is

Mixing console view. Channel strips with faders, meters, pan, mute/solo, sends.

## Dependencies
- `document-model` (Tier 0), `inspector` (Tier 2) for parameter widgets

## Definition of Done
- [ ] Channel strips render for all output-producing nodes
- [ ] Faders control gain
- [ ] Meters show real-time levels (data from diagnostics via bridge)
- [ ] Mute/solo work
- [ ] Pan control works
