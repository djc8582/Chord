[package]
name = "chord-plugin-host"
version = "0.1.0"
edition = "2021"

[dependencies]
chord-dsp-runtime = { path = "../dsp-runtime" }
chord-audio-graph = { path = "../audio-graph" }

[dev-dependencies]
tempfile = "3"
