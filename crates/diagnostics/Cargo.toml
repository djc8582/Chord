[package]
name = "chord-diagnostics"
version = "0.1.0"
edition = "2021"

[dependencies]
chord-dsp-runtime = { path = "../dsp-runtime" }
chord-audio-graph = { path = "../audio-graph" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
