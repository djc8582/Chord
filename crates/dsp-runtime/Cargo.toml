[package]
name = "chord-dsp-runtime"
version = "0.1.0"
edition = "2021"

[dependencies]
chord-audio-graph = { path = "../audio-graph" }

[dev-dependencies]
chord-node-library = { path = "../node-library" }
